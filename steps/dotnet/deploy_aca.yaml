steps:
  - task: AzureCLI@2
    inputs:
      azureSubscription: '$(azureServiceConnection)'
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      inlineScript: 'az containerapp update --container-name irasservice --image crrspshared2i2oqdevuks.azurecr.io/rsp-irasservice:$(semver.majorMinorPatch) --resource-group rg-rsp-applications-spoke-dev-uks --revision-suffix $(semver.major)-$(semver.minor)-$(semver.patch) --name irasservice'