steps:
  - task: AzureCLI@2
    inputs:
      azureSubscription: '$(azureServiceConnection)'
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      inlineScript: 'az containerapp update --container-name $(containerAppName) --image crrspshared2i2oqdevuks.azurecr.io/$(acrContainerName):$(Build.BuildId) --resource-group rg-rsp-applications-spoke-dev-uks --revision-suffix $(Build.BuildId) --name $(containerAppName)'